<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ir.iotacademy.gardenbalcony">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".FireApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Home" />
        <activity
            android:name=".JustValue"
            android:label="@string/title_activity_just_value" />
        <activity
            android:name=".ChartDashboard"
            android:label="@string/title_activity_chart_dashboard" />
        <activity
            android:name=".MixedDashboard"
            android:label="@string/title_activity_mixed_dashboard" />
        <activity
            android:name=".Notifications"
            android:label="@string/title_activity_notifications" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".GraphicalView" />
        <activity android:name=".Middle_View" />
        <activity android:name=".GraphicalViewRight" />
        <activity android:name=".ErrorDashboard"></activity>
    </application>

</manifest>